%h1 Listing albums

%table
  %thead
    %tr
      %th Name
      %th User
      %th
      %th
      %th

  %tbody
    - @albums.each do |album|
      %tr{data: {id: album.id}}
        %td= album.name
        %td= album.user
        %td= link_to 'Show', album, remote: true
        %td= link_to 'Edit', edit_album_path(album), remote: true
        %td= link_to 'Destroy', album, remote: true, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Album', new_album_path, remote: true, class: 'btn btn-primary'

#formModal.modal.fade
  .modal-dialog
    .modal-content



