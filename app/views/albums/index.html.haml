%h1 Listing albums

%table
  %thead
    %tr
      %th Name
      %th User
      %th
      %th
      %th

  %tbody
    - @albums.each do |album|
      %tr{data: {id: album.id}}
        %td= album.name
        %td= album.user
        %td= link_to 'Show', album
        %td= link_to 'Edit', edit_album_path(album)
        %td= link_to 'Destroy', album, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Album', new_album_path

%button.btn.btn-primary{ data: { target: "#formModal", toggle: "modal" }, type: "button" }
  Launch demo modal

#formModal.modal.fade
  -#{"aria-hidden" => "true", "aria-labelledby" => "exampleModalLabel", :role => "dialog", :tabindex => "-1"}
  .modal-dialog
    .modal-content
      .modal-header
        %h5.modal-title Modal title
        %button.close{data: {dismiss: "modal"}, type: "button"} ×
      .modal-body
        Body
      .modal-footer
        %button.btn.btn-secondary{data: {dismiss: "modal"}, type: "button"} Close
        %button.btn.btn-primary{:type => "button"} Save changes

